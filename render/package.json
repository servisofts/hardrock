{
  "name": "render-hardrock",
  "version": "2.3.2",
  "description": "render-hardrock",
  "main": "index.js",
  "types": "index.d.ts",
  "author": "servisofts",
  "license": "ISC",
  "dependencies": {
    "@react-navigation/native": "^5.9.6",
    "@react-navigation/stack": "^5.14.7"
  },
  "peerDependencies": {
    "react": "*",
    "react-dom": "*"
  },
  "devDependencies": {
    "@types/react": "^17.0.17",
    "typescript": "^4.1.3"
  },
  "scripts": {
    "up": "npm run build && cd dist && npm publish",
    "start": "rm -rf dist  && mkdir dist && cp package.json dist/package.json && tsc -p ./run.tsconfig.json",
    "build": "rm -rf dist  && mkdir dist  && cp package.json dist/package.json && tsc -p ./tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "*",
    "**"
  ]
}
